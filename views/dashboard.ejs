<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h2><%= header %></h2>
    <ul>
        <% profiles.forEach(function(profile) { %>
            <li><%= profile.fullName %> - <%= profile.contact %></li>
        <% }); %>
    </ul>
</body>
</html>
